import 'package:flutter/material.dart';

abstract class AppTheme {
  static ThemeData themeData = ThemeData(
      backgroundColor: const Color(0xFF030011),
      textTheme: const TextTheme(bodyText2: TextStyle(color: Colors.white)));
}
